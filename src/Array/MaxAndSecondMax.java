package Array;
/*
Given an array arr[] of size N of positive integers which may have duplicates.
 The task is to find maximum and second maximum from the array,
 and both of them should be distinct, so If no second max exists, then second max will be -1
 */
public class MaxAndSecondMax {
    public static void main(String[] args) {
        MaxAndSecondMax obj = new MaxAndSecondMax();
        System.out.println(obj.solve());
    }

    private int solve() {
        return 0;
    }
}
